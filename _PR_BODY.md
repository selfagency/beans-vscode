chore(beans): enforce branch + bean usage in Copilot templates

This is the draft PR body. It updates Copilot templates to require agents to create/switch to issue branches, keep todos in bean `## Todo`, use MCP/extension for bean edits, and only set `branch` and `pr` frontmatter keys.
